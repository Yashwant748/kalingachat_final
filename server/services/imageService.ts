/**
 * Image Generation Service
 * 
 * Disabled to maintain lightweight laptop performance.
 */

export async function generateImage(prompt: string, onProgress?: (msg: string) => void): Promise<string> {
    return "Image generation will be added in a future update.";
}
